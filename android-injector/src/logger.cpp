#include "logger.h"

// Placeholder. Currently the logger is header-only via the ALOG* macros.
// This .cpp is kept so the cmake target has at least one logger TU; future
// helpers (ring buffer for in-overlay log, level filtering, etc.) live here.
